<?php
/**
 * Шаблон конфига. Настоящий файл лежит НА СЕРВЕРЕ вне webroot и вне git:
 *   /var/www/<логин>/data/config/krovlya.php
 * Репозиторий публичный — пароль от базы и токен бота в него попасть не должны.
 *
 * Деплой этот файл не трогает: правится руками, переживает выкатки.
 */
return [
    // MySQL хостинга. Логин/пароль — из раздела «Доступы» в личном кабинете.
    'db_host' => 'localhost',
    'db_name' => 'u3616302_default',
    'db_user' => 'u3616302_default',
    'db_pass' => '',

    // Куда падают заявки письмом. Адресов может быть несколько — строкой через
    // запятую или массивом; каждому уходит своё письмо. На бою это ящик клиента
    // и ящик Максима: клиент читает заявки, Максим видит, что реклама работает.
    'mail_to'   => 'client@example.ru, manager@example.ru',
    // Отправитель — ящик на домене сайта, заведённый в ispmanager: тогда у письма
    // есть SPF домена и Яндекс с mail.ru не кладут его в спам.
    'mail_from' => 'sayt@example.ru',

    // Дублирование в Telegram. Пусто — заявка просто останется в базе.
    'telegram_token'   => '',
    'telegram_chat_id' => '',

    // PDF лид-магнитов. ВНЕ webroot: скачать можно только по одноразовому токену.
    'files_dir' => '/var/www/u3616302/data/files',
];
